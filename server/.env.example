# GLP-1 Coach — AI proxy (SERVER) environment
# Copy to ".env" on the server / set in your host's env settings.
# This key NEVER ships in the mobile app (report A1).

# Required: your OpenAI secret key. Lives only on the server.
OPENAI_API_KEY=

# Optional: port for the standalone Express server (default 8787).
PORT=8787
